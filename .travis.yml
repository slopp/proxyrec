language: node_js
node_js:
  - "8"

cache:
  directories:
    - "node_modules"

addons:
  artifacts:
    s3_region: "us-west-2"
    debug: true
    paths:
      - $HOME/build/slopp/proxyrec/build/main-macosx
      - $HOME/build/slopp/proxyrec/build/main-linux
